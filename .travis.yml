language: go

go:
  - 1.10.x
  - 1.11.x
  - 1.12.x
  - 1.13.x
  - tip

branches:
  only:
  - master
  - develop

os:
  - linux

matrix:
  fast_finish: true
  allow_failures:
    - go: tip

before_install:
  - make deps
  - make deps-test
  - go get -v github.com/mattn/goveralls
  - npm install -g codeclimate-test-reporter

script:
  - make all
  - go test -covermode count -coverprofile cover.out ./parser
  - goveralls -service travis-ci -repotoken $COVERALLS_TOKEN -coverprofile cover.out || exit 0
  - codeclimate-test-reporter < cover.out || exit 0
